Ordinary CommonMark, with *emphasis*, **strong** text, a `code span` and a
[plain link](https://example.com/a). An autolink reads
<https://example.org/> and an entity reads &amp;.

## A section

The heading above is an h2, so it is numbered 1.

### A subsection

The heading above is an h3, so it is numbered 1.1.

#### A deeper heading

The heading above is an h4, so it is numbered 1.1.1.

> A quotation.
> It runs to two lines.

- one
- two

1. first
2. second

```ocaml
let render x = x
```

***

## A second section

The heading above restarts the h3 counter, so it is numbered 2.

Text with a hard break at the end of this line,\
and the line that follows it.
