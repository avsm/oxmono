An image whose target is a bushel slug:

![A sample image](:sample-image)

The variants come from the image entry the test builds, not from any file
on disk, so this renders with no image data present.

Text after the image.
