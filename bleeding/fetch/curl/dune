(copy_files ../httpz/gzip_stream.ml)

(library
 (name fetch_curl)
 (public_name fetch-curl)
 (private_modules gzip_stream)
 (libraries fetch fetch.cookies eio eio.unix curl fmt cstruct httpz
   checkseum.c decompress.de decompress.gz))
