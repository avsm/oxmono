(mdx
 (package fetch-macos)
 (enabled_if
  (= %{system} macosx))
 (deps
  (package duration)
  (package mtime)
  (package base64)
  (package cmarkit)
  (package fetch-macos)
  (package fetch)
  (package httpz)
  (package uunf)
  (package uriz)
  (package jsont)))

(copy_files ../exchange.ml)

(test
 (name test_exchange)
 (modules test_exchange exchange)
 (package fetch-macos)
 (libraries eio eio_main)
 (action (run timeout 10s %{exe:test_exchange.exe})))
