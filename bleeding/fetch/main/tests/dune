(mdx
 (package fetch-main)
 (enabled_if
  (= %{system} macosx))
 (deps
  (package duration)
  (package mtime)
  (package cmarkit)
  (package fetch-main)
  (package fetch)
  (package httpz)
  (package uunf)
  (package jsont)))
