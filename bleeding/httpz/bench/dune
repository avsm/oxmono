(vendored_dirs vendor)

; Comparative benchmark: httpz (OxCaml) vs httpe (Eio)
; Uses simple timing + GC measurement (no core_bench to avoid Eio conflict)

(executable
 (name bench_compare)
 (libraries stdio stdlib_upstream_compatible unix httpz httpe eio eio_main base))

; Per-operation allocation for conditional-request fields and chunked decoding.

(executable
 (name bench_alloc)
 (libraries stdio stdlib_upstream_compatible unix httpz base))
