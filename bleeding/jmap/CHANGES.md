# Changes

- Add typed calendar codecs and Chain reads, with source-preserving archival
  reads through the ordinary response decoder.

- Add a storage-independent mirror state machine with validated cursor codecs.
