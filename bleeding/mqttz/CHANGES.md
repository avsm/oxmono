# Unreleased

Encode outgoing headers once into bytes, preserve sessions after oversized
requests, validate CONNECT settings and expose a composable TOML codec.

Port mqtte as mqttz with borrowed byte payloads, checked local parsing,
validated MQTT 3.1.1/5.0 packets, an Eio client, verified TLS and strict TOML.
