# Unreleased

- Accept caller-owned Fetch capabilities, bound response decoding and disable
  redirects. Expose the BibTeX codec with string source labels and remove the
  curl and Fpath dependencies.
